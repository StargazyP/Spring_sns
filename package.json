{
  "name": "spring-sns-webhook",
  "version": "1.0.0",
  "description": "Webhook server for Spring SNS deployment",
  "main": "webhook-server.js",
  "scripts": {
    "start": "node webhook-server.js",
    "pm2:start": "pm2 start webhook-server.js --name webhook-server",
    "pm2:stop": "pm2 stop webhook-server",
    "pm2:restart": "pm2 restart webhook-server",
    "pm2:logs": "pm2 logs webhook-server"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
